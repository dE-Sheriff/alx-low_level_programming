#include <stdio.h>

int main(void)
{
        char ch = 'a';
        while (ch <= 'z')
                putchar(ch);
		ch++

        char CH = 'A'
        while (CH <= 'Z')
                putchar(CH);
		CH++
        return (0);
}
