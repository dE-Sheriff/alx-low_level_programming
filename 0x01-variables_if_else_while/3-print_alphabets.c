#include <stdio.h>

int main(void)
{
        char ch = 'a';
	/* using while loop */

        while (ch <= 'z')
	{
		putchar(ch);
		ch++;
	}

        char CH = 'A'
	/* Another while loop */

        while (CH <= 'Z')
	{
                putchar(CH);
		CH++;
	}
        return (0);
}
