#include <stdio.h>

int main(void)
{
        char ch = 'a';
        while (ch <= 'z')
                putchar(ch);

        char ch = 'A'
        while (ch <= 'Z')
                putchar(ch);
        return (0);
}
